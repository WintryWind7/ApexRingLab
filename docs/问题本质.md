# 问题本质

## 核心认知

模型希望实现的并不是一个精确的线性问题，即不是通过点预测点，而是判断大概率的位置。

## 问题特性

输入不是绝对精确的。可以理解为：数据集里面，同样的 Ring1 位置，也会有可能刷在其他地方，只是记录的这一次恰好刷在这里。

## 对建模的影响

给定 Ring1 和 Ring2 的位置，Ring3 的刷新位置不是确定性的，而是一个概率分布。同样的输入可能对应多个不同的输出，数据集中记录的只是其中一次实际发生的情况。
